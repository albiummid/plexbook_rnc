import LinearGradient from 'react-native-linear-gradient';
import {createShimmerPlaceholder} from 'react-native-shimmer-placeholder';

const Skelton = createShimmerPlaceholder(LinearGradient);

export default Skelton;
